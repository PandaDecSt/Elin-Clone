# 2026-07-21 工作日志

## tile 数值含义破解（来自 Elin-Decompiled 源码）⭐ 重大突破
- **关键函数**：`Elin-Decompiled/Elin/RenderData.cs::ConvertTile()` 第111-114行
  ```csharp
  return tile / 100 * pass.pmesh.tiling.x + tile % 100;
  ```
  → **tile 不是线性序号，而是「行×100 + 列」坐标编码！**
  - `col = tile % 100`, `row = tile // 100`
  - 像素: `x = col*cellW`, `y = row*cellH`
- **图集参数（已像素级验证）**：
  - floors.png: 2048×1920, **cellW=64 cellH=48**, cols=32 rows=40（行边界透明度测试：cellH=48边界带alpha仅占全图2% vs cellH=64的102%，确凿）
  - blocks.png: 2048×2048, **cellW=64 cellH=64**, cols=32 rows=32
- **修正之前的误判**：之前认为 floors/blocks 是"打包图集"（可变尺寸）→ 实际是**规则网格图集**，连通域测到的"可变尺寸"是精灵内容大小不同而非cell不规整。data.js 的真正 bug 是把 tile 当线性序号用 `bid%32` 读。
- 已生成权威映射表和视觉验证图（_verify_floors/blocks.png），解码完全正确。
- 反编译源码已抓取到 `data/elin_source/src_key/`（TileType/MeshPass/RenderData/SpriteSheet/SourceFloor/SourceBlock 等）

## map-viewer.html / data.js 重写完成 ✅
- **解码链路（两步）**：casino_render.json 的 floors/blocks 存的是**材质 id**（如 floor 90、block 160），不是 tile。
  - 材质 id → tile：查 SourceBlock 映射（内嵌 `FLOOR_ID_TILE`/`BLOCK_ID_TILE`，由 floor_map/block_map 生成，95/87 条，0 不一致）
  - tile → 像素：`col=tile%100, row=tile//100`，floors cell 64×48 / blocks cell 64×64
- **map-viewer.html 改动**：删掉错误的「列数滑块」和 `bid%32/fid%cols` 网格假设；改为内嵌 id→tile 表 + `tileRect()` 正确解码；tile=0 用 hasOwnProperty 严格判断（否则 143/103 等首页格被误判缺映射）；缺映射 id 回退为纯色/占位块。
- **data.js 改动**：`BLOCK_TYPES`/`FLOOR_ATLAS` 的失效 r/c 全换成 tile 编号（带正确解码注释）；新增共享 `decodeTile(tile,atlas)` 工具；`GRASS_ATLAS` 注释修正为 floors.png 网格坐标。
- **Node 审计通过**：内嵌表与源 0 不一致；casino 所有 floor/block id 解码零越界；tile=0 修复后正常。
- **真实缺口（源表解析本身缺 tile，非解码问题）**：casino 用到的 floor 34/35/76、block 103(玻璃墙)/153 在已解析 map 里无 tile 值，回退占位块。补全需原始 SourceBlock xlsx 对应行（属另一项「补全源表」任务）。
- 本地服务 8123 验证：map-viewer.html / data.js / casino_render.json / floors.png / blocks.png 全部 200。

## Elin 贴图映射数据「怎么读进游戏 / 读什么文件」调研 ✅
- **入口**：`SourceManager.Init()`（`Core.Instance.sources`）按字段反射收集所有 `SourceData`，依次调 `blocks.Init()/floors.Init()/objs.Init()/decos.Init()/things.Init()`… 每个 `SourceData.Init()` 读对应的 Source Sheet。
- **逻辑层文件 = Source Sheets**：xlsx（sheet名=类名：Block/Floor/Obj/Deco/Thing…），行格式 第1 header / 第2 type / 第3 default / 第4起数据；`RowMapping`(列名→列序号) 定顺序。`SourceBlock.CreateRow()` 用 `SourceData.GetInt/GetString/GetIntArray(列号)` 解析当前行。`tiles` 列=贴图编号数组（每方向/变体一个），如 `123,-123,456,-456`。
- **像素层 = RenderData + 图集**：`RenderRow.SetRenderData()` 用 `ResourceCache.Load<RenderData>("Scene/Render/Data/"+_idRenderData)` 载入 ScriptableObject。`RenderData.pass`(MeshPass)→`mat._MainTex`=图集PNG；`pass.pmesh.tiling`(Vector2)=网格行列数。
- **tile→像素换算（源码级确认）**：`RenderRow.SetTiles()` 与 `RenderData.ConvertTile()` 同一式 `_tiles[i]=tiles[i]/100*pmesh.tiling.x + tiles[i]%100` ⇒ tile=行×100+列，列数=tiling.x（floors/blocks 均32列）。`RenderRow.GetSprite()` 切图：`cellW=texW/tiling.x, cellH=texH/tiling.y`，`Rect(col*cellW, texH-(row+1)*cellH, cellW, cellH)`。
- **mod 替换**：`Texture Replace/<图集名>_<tile编号>.png`（如 obj_123.png）覆盖单格；`@obj` 自定义放 `Texture/<id>.png`；`.pref`(INI) 微调 pivot/阴影/缩放。
- **结论**：逻辑映射(xlsx)→JSON 我们已有；tile→rect 的权威来源是游戏内 RenderData 的 `tiling`+图集尺寸（floors 32×40 cell64×48 / blocks 32×32 cell64×64），已与之前 MEMORY 修正一致。本地 assets/elin 只有 PNG 无 tiling 元数据，故精确 rect 仍需纹理查看器/解析 RenderData 资源。

## objs 摆件 sprite rect 解析 + map-viewer 物件层 ✅
- **build_objs_rects.py** 三层查找：Tier1 精确格(340) → Tier2 邻居救援最近中心+min8px(20) → missing
- 排除伪纹理 objs_SS/objs_C/CL/CLL（speck 误报源）；per-candidate cell 尺寸距离阈值
- 最终 360/360 tile 找到；视觉验证 19/20 救援正确，id87 wreck(9×10 speck) 排除
- **obj_id_rect.js**: 145/147 obj ID 有效 lookup（缺 id87 wreck）
- 救援目标多在 (col,row-1) 上行 — 底部锚定对象 art 在 anchor cell 上方一格
- **map-viewer.html** 新增 objs 层：加载 objs_S/objs/objs_L 纹理，画家排序 (x+y)，底部锚定渲染
- Casino 地图物件 id4(白花)/14(crim)/50(berry) 已可渲染；info 面板显示 obj 详情

## BUG修复：map-viewer.html 加载崩溃
- **症状**：`JSON.parse` 解析 `obj_id_rect.js` 失败（`{4:{tex:...}}` 非合法 JSON，键未引号）
- **修复**：新增 `data/elin_source/obj_id_rect.json`（合法 JSON，含 `OBJ_ID_RECT` + `OBJ_TILE_RECTS`）；
  viewer 改为 `fetch(...).json()` 直接加载；`state.objRects = objRectRaw.OBJ_ID_RECT`
- **深度修正**：cards 与 objs 合并到同一 `drawList` 按 (x+y) 画家排序，避免花朵盖在家具上
- `obj_id_rect.js` 保留给 game.js/iso.js/world.js 游戏引擎消费（仍是合法 JS 对象字面量）

## 更正：游戏运行时读的是 xlsx，不是 txt（已源码+官方文档双确认）
- 我口头答过"游戏读导出的纯文本 txt"是**误述**，更正如下（官方 Basic Sheet Modding 指南原文："The formatted xlsx files add SourceData into the game" / "place your mod data — an xlsx file. This xlsx file will act as your Source Sheet"）。
- **运行时主加载格式 = xlsx**：Modder 把 `.xlsx` 放进 mod 包的 `EN/`（或 JP/CN）语言文件夹，游戏直接读 xlsx 作为 Source Sheet。`SourceManager.Init()`→`blocks.Init()` 等读的就是 xlsx。xlsx 本质 zip 包内 XML，由游戏代码解析。
- **`ImportSourceTexts/ExportSourceTexts(path)/UpdateSourceTexts` 是另一条文本工具通道**（开发者/debug/启动参数用），不是 Modder 主路径；不要把它误当成"游戏读 txt"。
- **xlsx 导出是谁做的**：① 编辑=电子表格软件（Excel/LibreOffice Calc/Google Sheets）；② 游戏原生读 xlsx，无"xlsx→txt 转换"步骤；③ 教程"export the Source sheets via launch arguments"=**游戏代码**把官方源表 dump 成 xlsx 给 Modder 当模板，不是 Excel 的"另存为"。
- **对 H5 复刻**：解析 xlsx→JSON 正好对上游戏真实源格式，方向正确；运行时吃 JSON（构建期解析掉，零成本）。

## Elin xlsx SourceData 移植进 H5（用户指令：抄源码把xlsx读取移植+各种属性读取应用+保证有中文）
- **构建期**：`data/elin_source/build_sources.py` 读取 4 张 xlsx（SourceBlock/SourceCard/SourceChara/Lang），按 Elin 约定 row1=列名/row2=类型/row3+=数据，类型还原(int/float/bool/string/color/elements/数组)，alias 列合并进目标行 → 输出 `data/elin_source/sources.json`（4402 实体行 + 4303 Lang 条目）。
- **中文**：源数据只有 name_JP(日文)/name(英文)/Lang.text(英文)，**无中文列**。`data/elin_source/build_lang_zh.py` 生成 `lang_zh.json`（71 条：10种族/12职业/32物品/17怪物），由"当前 data.js 已用中文名"+"Source 可玩内容权威翻译"合并。运行时 `ZH(sheet,id)` = lang_zh 覆盖 → en → jp。
- **运行时**：`js/source-data.js` 复刻 Elin Source 体系：`Source.Get(file,sheet,id)` / `Source.List` / `Lang.Get` / `ZH`，及归一化访问器 `getRace/getJob/getThing/getChara/getMaterial/getFood`（优先 Source，回退 data.js 常量，绝不破坏现有玩法）。
- **接入玩法**：`createPlayer` 用 getRace/getJob（STR/END/DEX…→力量/体力/灵巧…，全部原始属性存 `.src`，ele* 元素注入 ElementContainer）；`makeItem` 用 getThing；`makeMonster` 用 enrichMonster 补充 Chara 原始数据+中文名+元素。`game.js` 启动 `await loadSource()`；`ui.js` 显示 raceName/className。
- **验证**：Node 集成测试 28/28 通过（中文 妖精/战士/匕首/史莱姆、fairy+warrior→力量13、回退 mage/hillfolk 正常）。演示页 `source-test.html`（http://localhost:8788/source-test.html）。
- **注意**：当前游戏 id 与 Source id 部分不同（mage≠wizard, hillfolk≠dwarf, executioner 无对应）→ 已用常量回退保证不破；若要全员 Source 驱动需改 UI 选择列表用 Source 可玩 id。
